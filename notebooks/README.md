# Notebooks

The exact latest notebook files are not included in this reconstructed code-only package because the current hardening branch commit exists only on the local Windows machine and has not yet been pushed to GitHub.

Keep these local files in this directory when applying the package:

- `01_eda.ipynb`
- `02_model_experiments.ipynb`

The hardened model code in `src/car_price/` is the reproducible training and prediction path.
